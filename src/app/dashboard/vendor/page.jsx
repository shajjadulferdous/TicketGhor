import Sidebar from '@/components/SideBar';
import React from 'react';

const page = () => {
    return (
        <div>
            this is the vendor page
        </div>
    );
};

export default page;